# EAIM 프로덕션 — 저장소에 넣는 방법 (한 장 요약)

저장소 파일 구조 (지금):

```
index.html          ← 플랫폼 허브 (maker / slideshow / audio-mixer / guide 카드)
maker.html          ← 뮤지컬메이커
slideshow.html      ← 슬라이드쇼 (rec · dialogue 애드온 이미 연결됨)
audio-mixer.html    ← 오디오 믹서
guide.html
slideshow-rec.js
slideshow-dialogue.js
```

## 새로 올릴 파일 (같은 폴더에)

```
production-media.js   ← 그림·노래 생성 (maker.html용)
mixer-import.js       ← 만든 노래 불러오기 (audio-mixer.html용)
slideshow-rec.js      ← 갱신본 (보관함 노래·그림 재생 추가) — 덮어쓰기
```

## 파일별 한 줄 추가

**maker.html** — `<head>` 안 firebase-auth-compat 다음 줄:
```html
<script src="https://www.gstatic.com/firebasejs/10.7.1/firebase-storage-compat.js"></script>
```
**maker.html** — `</body>` 바로 앞:
```html
<script src="production-media.js"></script>
```
**audio-mixer.html** — `</body>` 바로 앞:
```html
<script src="mixer-import.js"></script>
```
**slideshow.html** — 이미 되어 있음. `slideshow-rec.js`만 새 파일로 덮어쓰기.

## 이제 이렇게 흐릅니다

1. **maker.html** 학생 화면: 넘버마다 🎵 30초 / 🎼 전체 곡, 배경 프롬프트마다 🎨 그림
   → 결과는 **이 기기 보관함**(브라우저 IndexedDB)에 자동 저장 (Storage 설정이 없어도 됨)
2. **slideshow.html**: 🎬 슬라이드쇼로 넘어가면 배경 그림이 깔리고, 장면마다 그 넘버가 재생됨
   ⏱ "장면별 노래에 맞추기"로 두면 노래가 끝날 때 다음 장면으로
3. **audio-mixer.html**: 오른쪽에 **🎭 뮤지컬메이커 노래** 목록. 클릭 한 번으로 BGM/MR/AMB 채널에,
   "큐에 자동 배치"를 누르면 오프닝·N막·커튼콜 큐의 MR에 그 넘버가 연결됨

보관함은 **같은 브라우저** 안에서만 공유됩니다. 학생이 다른 기기에서 이어서 하려면
maker에서 "노래 저장"으로 내려받아 믹서에 올리거나, Firebase Storage를 켜서 원격 URL로 쓰면 됩니다.

## Suno → Gemini(Lyria) 전환

`production-media.js`가 대본 생성 지시문의 "영문 Suno 프롬프트" 요구를
"노래 스타일 지시문(장르·악기·보컬·분위기·BPM)"으로 바꿔 보내고, 화면의 "Suno" 글자도
"노래 스타일"로 바꿉니다. maker.html 본체는 손대지 않아도 됩니다.
